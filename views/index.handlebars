<h1>Cats!</h1>

<h2>Cats that are not adoptable!</h2>
<ul>
    {{#each cats}}
        {{#unless adoptable}}
            {{> cats/cat-block adopt=true}}
        {{/unless}}
    {{/each}}
</ul>


<h2>Cats that are adoptable!</h2>
<ul>
    {{#each cats}}
        {{#if adoptable}}
            {{> cats/cat-block adopt=false}}
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Cat!</h2>

<form class="create-form">
    <div class="form-group">
        <label for="ca">Cat Name:</label>
        <input type="text" id="ca" name="name">
    </div>
    <div class="form-group">
        <label for="adoptable">Have they been adopted?</label><br>
        <input type="radio" name="adoptable" value="1" checked>Adoptable<br>
        <input type="radio" name="adoptable" value="0">Adopted!
    </div>

    <button type="submit">Add Cat</button>
</form>